<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xinmengqaq.springboot.user.mapper.BlogUserMapper">


    <!--复用字段列-->
    <sql id="blogUserColumns">
        id, email, password, nickname, avatar, status,
        password_version,version, delete_at, created_at, updated_at
    </sql>

    <!--根据id查询用户，加锁-->
    <select id="selectByIdForUpdate" resultType="BlogUser">
        SELECT <include refid="blogUserColumns"/>
        FROM blog_user
        WHERE id = #{id}
        FOR UPDATE
    </select>

    <!--根据邮箱查询用户，加锁-->
    <select id="selectByEmailForUpdate" resultType="BlogUser">
        SELECT <include refid="blogUserColumns"/>
        FROM blog_user
        WHERE email = #{email}
        FOR UPDATE
    </select>

    <!--删除过期的待激活用户-->
    <delete id="deleteExpiredPendingUsers">
        DELETE FROM blog_user
        WHERE id IN (
        SELECT id FROM blog_user
        WHERE status = 'pending_deletion'
        AND delete_at &lt;= #{now}
        LIMIT #{limit}
        )
    </delete>

</mapper>